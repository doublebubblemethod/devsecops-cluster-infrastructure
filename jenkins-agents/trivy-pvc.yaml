apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trivy-pv-claim
  namespace: jenkins
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
